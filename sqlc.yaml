version: "2"
sql:
  - engine: "postgresql"
    schema: "migrations"
    queries: "internal/db/queries"
    gen:
      go:
        package: "storeq"
        out: "internal/db/storeq"
        sql_package: "database/sql"
        emit_interface: true
        overrides:
          - db_type: "uuid"
            go_type: "string"
